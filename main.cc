#include "Database.h"
#include "Key.h"
#include "Value.h"
#include <iostream>

int main() {
  Database d;
  //d.write(Key(1), Value(3));
  //d.write(Key(10), Value(100));
  //Value v = d.read(Key(1));
  //std::cout << v.get_is_delete();
  //std::cout << v.get();
  //v = d.read(Key(2));
  //std::cout << v.get_is_delete();
  //std::cout << v.get();
  return 0;
}
